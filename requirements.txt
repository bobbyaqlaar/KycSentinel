# KYC Sentinel tenant deps.
#
# The AgentSmith runtime is a real dependency now (framework G6) rather than
# something bootstrapped onto sys.path. Pin it by tag/sha for reproducible
# builds; `pip install -e ../AgenticFramework` for local development.
#
#   agentsmith-runtime[temporal,postgres,hitl] @ git+https://github.com/bobbyaqlaar/AgentSmith@v1.0.0
#
# Left commented while the framework repo is local-only — CI installs it from
# the checked-out framework path (see .github/workflows/ci.yml).
# agentsmith-runtime[temporal,postgres,hitl] @ git+https://github.com/bobbyaqlaar/AgentSmith@v1.0.0

# Direct deps of this app's own code (the runtime brings its own).
pydantic>=2.0,<3.0
pyyaml>=6.0,<7.0
temporalio>=1.5,<2.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
